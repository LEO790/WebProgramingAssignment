-- Run this first in phpMyAdmin (SQL tab), then run schema.sql, or paste both in order.
CREATE DATABASE IF NOT EXISTS web_programing_assignment
    CHARACTER SET utf8mb4
    COLLATE utf8mb4_unicode_ci;
